{
  "name": "ai-demo-repo",
  "version": "1.0.0",
  "description": "Life OS — AI-powered workflow orchestration platform",
  "private": true,
  "workspaces": [
    "apps/mobile",
    "packages/*"
  ],
  "scripts": {
    "dev:mobile": "cd apps/mobile && npx expo start",
    "dev:api": "cd apps/api && uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "lint:mobile": "cd apps/mobile && npx tsc --noEmit",
    "test:api": "cd apps/api && python -m pytest tests/ -v"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alikamatu/ai-demo-repo.git"
  },
  "bugs": {
    "url": "https://github.com/alikamatu/ai-demo-repo/issues"
  },
  "homepage": "https://github.com/alikamatu/ai-demo-repo#readme",
  "license": "ISC",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
